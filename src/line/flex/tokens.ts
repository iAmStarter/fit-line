/**
 * Semantic design tokens for LINE Flex cards (OVERVIEW §4, PLAN Phase 1).
 * Single source — do not scatter hex literals across builders.
 */

/** Success / recorded confirmation. */
export const SUCCESS_COLOR = '#1e9e57';
export const SUCCESS_TINT = '#e5f7f0';

/** Error / reject / block notices. */
export const ERROR_COLOR = '#d64545';

/** Info / guidance / on-demand reports. */
export const INFO_COLOR = '#2f6fed';

/** Secondary metadata, chart labels. */
export const MUTED_COLOR = '#666666';

/** Bar chart fill (aligns with success theme). */
export const CHART_BAR_COLOR = '#1e9e57';
